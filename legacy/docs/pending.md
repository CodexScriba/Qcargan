## Supabase Email Templates — TODO

- Audit current templates (confirm signup, magic link, reset password, change email).
- Unify branding (logo, colors, typography) to match app theme.
- Localize ES/EN content and subjects; link to locale-aware routes.
- Improve copy for clarity and phishing resistance; include support link.
- Add accessible markup (semantic headings, larger tap targets, alt text).
- Inline CSS for broad client support; test dark/light variants.
- Verify links: `Site URL`, reset redirect to `/auth/update-password` per locale.
- Add preview/test flow (staging project or template JSON backup in repo).
- Document template variables and update steps in `README.md`.
